export enum Lights {
    night = "NIGHT_LIGHT",
    wake = "WAKE_LIGHT"
}

export enum LightsGPIOPin {
    NIGHT_LIGHT = 27,
    WAKE_LIGHT = 22
}

export enum LightsDigitalState {
    ON = 1,
    OFF = 0
}
