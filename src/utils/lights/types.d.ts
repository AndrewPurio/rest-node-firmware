export enum Lights {
    night = "NIGHT_LIGHT",
    wake = "WAKE_LIGHT"
}

export enum LightsGPIOPin {
    NIGHT_LIGHT = 13,
    WAKE_LIGHT = 15
}

export enum LightsDigitalState {
    ON = 1,
    OFF = 0
}